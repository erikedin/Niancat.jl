using ExecutableSpecifications

@given("a puzzle {word}") do context
    @fail "Implement me"
end


@when("a guess {word} is made") do context
    @fail "Implement me"
end


@then("the response is that the guess is wrong") do context
    @fail "Implement me"
end


@then("the response is that the word is correct") do context
    @fail "Implement me"
end


@given("a dictionary") do context
    @fail "Implement me"
end
